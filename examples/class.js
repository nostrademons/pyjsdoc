/**
 * This is a class documentation comment, distinguished from a module or
 * function comment by the presence of the class tag.  Since the first
 * doc comment is a class comment, the module documentation is assumed to
 * be empty.
 *
 * @class MyClass
 */
window.MyClass = Class.create({

    /**
     * Methods must be explicitly associated with a class through the member
     * tag.  In the future, we may come up with a way to automatically
     * associate them through curly-brace positioning, but not yet.
     *
     * @member MyClass
     * @param arg1 The first argument
     * @param arg2 The second argument
     * @return Some computation.
     */
    first_method: function(arg1, arg2) {}

});
